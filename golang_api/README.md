# О программе

Данная API разрабатывалась с целью выполнения индивидуального задания по учебной практике.

Тема индивидуального задания:
  Разработка программного обеспечения по учету расходных материалов офисной техники в организации

Все права защищенны ©

# Office Supplies Management API

Система учета расходных материалов для офисной техники с REST API на Go и SQLite.

# Установка и запуск



# Установка зависимостей

В корневой директории проекта выполняем команду:
  ```bash 
  
  go mod tidy
  
  ```
# Запуск сервера

Команда на запуск самого API:
  ```bash 
  
  go run main.go
  
  ```

Сервер запустится на `http://localhost:5600` (Порт можно изменить в исходниках)

### Проверка работоспособности
  ```bash

  curl http://localhost:5600/health
  
  ```

  Ответ:
  ```json
  {
    "status": "OK",
    "database": "connected"
  }
  ```

## 📚 API Endpoints

Краткая документация всех api эндпоинтов для разработчиков!

### Расходные материалы

#### Получить все материалы
**GET** `/supplies`

```bash
curl http://localhost:5600/supplies
```

Ответ:
```json
[
  {
    "id": 1,
    "name": "Картридж HP 85A",
    "type": "Картридж",
    "model": "HP LaserJet 85A",
    "quantity": 10,
    "min_quantity": 3,
    "unit": "шт",
    "location": "Склад А",
    "created_at": "2024-01-15T10:30:00Z",
    "updated_at": "2024-01-15T10:30:00Z"
  }
]
```

#### Получить материал по ID
**GET** `/supplies/{id}`

```bash
curl http://localhost:5600/supplies/1
```

#### Создать материал
**POST** `/supplies`

```bash
curl -X POST http://localhost:5600/supplies \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Бумага А4",
    "type": "Бумага",
    "model": "SvetoCopy",
    "quantity": 50,
    "min_quantity": 10,
    "unit": "пачка",
    "location": "Склад Б"
  }'
```

#### Обновить материал
**PUT** `/supplies/{id}`

```bash
curl -X PUT http://localhost:5600/supplies/1 \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Бумага А4 Premium",
    "quantity": 45,
    "min_quantity": 10
  }'
```

#### Удалить материал
**DELETE** `/supplies/{id}`

```bash
curl -X DELETE http://localhost:5600/supplies/1
```

#### Материалы с низким запасом
**GET** `/supplies/low-stock`

```bash
curl http://localhost:5600/supplies/low-stock
```

### Использование материалов

#### Записать использование
**POST** `/usage`

```bash
curl -X POST http://localhost:5600/usage \
  -H "Content-Type: application/json" \
  -d '{
    "supply_id": 1,
    "quantity_used": 2,
    "used_by": "Иванов И.И.",
    "department": "Бухгалтерия",
    "purpose": "Замена картриджа"
  }'
```

#### История использования
**GET** `/usage/history`

Параметры:
- `supply_id` - фильтр по материалу
- `department` - фильтр по отделу
- `start_date` - начальная дата (YYYY-MM-DD)
- `end_date` - конечная дата (YYYY-MM-DD)

```bash
# Вся история
curl http://localhost:5600/usage/history

# С фильтрами
curl "http://localhost:5600/usage/history?department=IT&start_date=2024-01-01"
```

### Запросы на пополнение

#### Получить все запросы
**GET** `/requests`

Параметры:
- `status` - фильтр по статусу (pending, approved, rejected, completed)

```bash
# Все запросы
curl http://localhost:5600/requests

# Только ожидающие
curl "http://localhost:5600/requests?status=pending"
```

#### Создать запрос
**POST** `/requests`

```bash
curl -X POST http://localhost:5600/requests \
  -H "Content-Type: application/json" \
  -d '{
    "supply_id": 1,
    "quantity": 5,
    "requested_by": "Петров П.П.",
    "department": "IT отдел"
  }'
```

#### Обновить статус запроса
**PUT** `/requests/{id}/status`

```bash
curl -X PUT http://localhost:5600/requests/1/status \
  -H "Content-Type: application/json" \
  -d '{"status": "approved"}'
```

**Важно:** При статусе `completed` количество материала автоматически пополняется.

### Статистика

#### Общая статистика
**GET** `/statistics`

```bash
curl http://localhost:5600/statistics
```

Ответ:
```json
{
  "total_supplies": 15,
  "total_items": 250,
  "low_stock_count": 3,
  "pending_requests": 2,
  "monthly_usage": [
    {
      "month": "2024-01",
      "amount": 45
    }
  ]
}
```

## 🗃️ Модели данных

### SupplyItem (Расходный материал)
```json
{
  "id": 1,
  "name": "Картридж HP 85A",
  "type": "Картридж",
  "model": "HP LaserJet 85A",
  "quantity": 10,
  "min_quantity": 3,
  "unit": "шт",
  "location": "Склад А",
  "created_at": "2024-01-15T10:30:00Z",
  "updated_at": "2024-01-15T10:30:00Z"
}
```

### UsageRecord (Запись использования)
```json
{
  "id": 1,
  "supply_id": 1,
  "quantity_used": 2,
  "used_by": "Иванов И.И.",
  "department": "Бухгалтерия",
  "purpose": "Замена картриджа",
  "used_at": "2024-01-15T12:00:00Z"
}
```

### SupplyRequest (Запрос на пополнение)
```json
{
  "id": 1,
  "supply_id": 1,
  "quantity": 5,
  "requested_by": "Петров П.П.",
  "department": "IT отдел",
  "status": "pending",
  "created_at": "2024-01-15T13:00:00Z",
  "updated_at": "2024-01-15T13:00:00Z"
}
```

## 🔧 Технические детали

### Технологии
- **Backend**: Go 1.21+
- **Фреймворк**: Gin
- **База данных**: SQLite (pure-Go драйвер)
- **CORS**: Встроенная поддержка

### Структура проекта
```
office_supplies/
├── main.go              # Точка входа
├── handlers/            # Обработчики API
│   └── handlers.go
├── models/              # Модели данных
│   └── models.go
├── middleware/          # Промежуточное ПО
│   └── middleware.go
└── database/            # Файлы БД
    └── office_supplies.db
```

### Статусы запросов
- `pending` - Ожидает рассмотрения
- `approved` - Одобрен
- `rejected` - Отклонен
- `completed` - Выполнен (запас пополнен)

## 🐛 Устранение неполадок

### Сервер не запускается
```bash
# Проверьте установку Go
go version

# Убедитесь что в директории есть go.mod
ls go.mod

# Очистите кэш модулей
go clean -modcache
go mod tidy
```

### Проблемы с CORS
- CORS уже настроен для всех origins
- Для Production замените `*` на конкретный домен

### Ошибки базы данных
- База данных создается автоматически
- Файл БД: `database/office_supplies.db`
- Схема инициализируется при первом запуске

## 📊 Пример рабочего процесса

1. **Добавление материала**
   ```bash
   POST /supplies
   ```

2. **Использование материала**
   ```bash
   POST /usage
   ```

3. **Создание запроса при низком запасе**
   ```bash
   POST /requests
   ```

4. **Одобрение и выполнение запроса**
   ```bash
   PUT /requests/1/status → "completed"
   ```

## 📄 Автор

Разработал студент группы ИСПт 22-(9)-1 Шишкин Николай Александрович 