# Office Supplies Management - React Client

React TypeScript клиент для системы учета расходных материалов офисной техники.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 16+ 
- npm или yarn
- Go API сервер (должен быть запущен на порту 5600)

### Установка и запуск

```bash
# Клонирование репозитория
git clone <repository-url>

# Установка зависимостей
npm install

# Запуск development сервера
npm run dev
```

### Сборка для production

```bash
# Сборка проекта
npm run build

# Просмотр собранной версии
npm run preview
```

## 🛠 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Статическая типизация
- **React Query (TanStack Query)** - Управление состоянием и кэширование
- **React Router** - Маршрутизация
- **Material-UI (MUI)** - Компоненты и дизайн система
- **Axios** - HTTP клиент
- **React Hook Form** - Управление формами
- **Vite** - Сборка и development сервер
- **Electron TS** - Фреймворк для десктоп приложений

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Layout.tsx      # Основной layout приложения
│   ├── Dashboard.tsx   # Панель управления
│   ├── SupplyList.tsx  # Список материалов
│   └── SupplyForm.tsx  # Форма создания материала
├── hooks/              # Кастомные React Query хуки
│   └── useSupplies.ts  # Хуки для работы с материалами
├── services/           # API клиент
│   └── api.ts         # Настройка axios и API методы
├── types/              # TypeScript типы
│   └── types.ts       # Интерфейсы данных
├── context/            # React контексты
│   └── SnackbarContext.tsx # Уведомления
└── App.tsx             # Корневой компонент
```

## 🎯 Функциональность

### 📊 Панель управления
- Общая статистика по материалам
- Количество материалов с низким запасом
- Общее количество единиц материалов

### 📦 Управление материалами
- Просмотр списка всех материалов
- Создание новых материалов
- Удаление материалов
- Визуальное отображение статуса запасов
- Фильтрация материалов с низким запасом

### 🔔 Особенности интерфейса
- **Цветовая индикация** статуса материалов
- **Уведомления** об успешных операциях и ошибках
- **Адаптивный дизайн** для мобильных устройств
- **Интуитивные формы** с валидацией

## 🔌 Интеграция с API

Клиент настроен на работу с Go API по адресу: `http://localhost:5600`

### Основные endpoints:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/supplies` | Получить все материалы |
| `POST` | `/supplies` | Создать материал |
| `DELETE` | `/supplies/{id}` | Удалить материал |
| `GET` | `/supplies/low-stock` | Материалы с низким запасом |
| `GET` | `/statistics` | Статистика |

### Примеры API вызовов:

```typescript
// Получить все материалы
const { data: supplies, isLoading } = useSupplies();

// Создать материал
const createMutation = useCreateSupply();
createMutation.mutate({
  name: "Картридж HP",
  type: "Картридж", 
  model: "HP 85A",
  quantity: 10,
  min_quantity: 3,
  unit: "шт",
  location: "Склад А"
});

// Удалить материал
const deleteMutation = useDeleteSupply();
deleteMutation.mutate(1);
```

## 🎨 Компоненты

### SupplyList
- Отображает таблицу с материалами
- Цветовая индикация низкого запаса
- Кнопки действий (удаление)
- Автоматическое обновление данных

### SupplyForm 
- Форма создания новых материалов
- Валидация полей
- Выпадающие списки для типов и единиц измерения
- Интеграция с React Hook Form

### Dashboard
- Статистические карточки
- Обзор системы
- Быстрый доступ к основным функциям

=
### Ошибка подключения к API
```bash
# Проверьте, что Go API запущен
curl http://localhost:5600/health

# Должен вернуть: {"status":"OK","database":"connected"}
```

### Проблемы с зависимостями
```bash
# Очистите кэш и переустановите
rm -rf node_modules
npm install
```

### TypeScript ошибки
```bash
# Проверьте типы
npx tsc --noEmit

# Или через Vite
npm run build
```


## 🔮 Планы развития

- [ ] Страница истории использования
- [ ] Управление запросами на пополнение
- [ ] Фильтрация и поиск материалов
- [ ] Экспорт отчетов
- [ ] Графики использования
- [ ] Многопользовательский режим
- [ ] PWA функциональность

## 🤝 Разработка

### Code style
```bash
# Проверка типов
npx tsc --noEmit

# Форматирование кода (если настроен Prettier)
npx prettier --write src/
```

### Структура коммитов
- `feat:` Новая функциональность
- `fix:` Исправление ошибок
- `docs:` Обновление документации
- `style:` Изменения кода, не влияющие на логику
- `refactor:` Рефакторинг кода

## 📄 Лицензия

MIT License

Разработал студент группы ИСПт 22 - (9) - 1 Шишкин Николай Александрович

## 💡 Быстрые команды

```bash
# Development
npm run dev

# Production сборка
npm run build

# Preview сборки
npm run preview

# Типовая проверка
npx tsc --noEmit
```